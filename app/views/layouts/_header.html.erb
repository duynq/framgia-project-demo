<nav class="navbar navbar-inverse navbar-fixed-top" style="border-radius:0px;">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <b><%= link_to "SOCIAL NETWORK", root_path, id: "logo", class: "navbar-brand" %></b>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
     <ul class="nav navbar-nav navbar-right">
      <% if logged_in? %>
      <li><%= link_to gravatar_for(current_user, size: 17) + " " + current_user.name, user_path(current_user) %></li>
      <li class="divider-vertical"></li>
      <li><%= link_to "Home", root_path %></li>
      <li class="divider-vertical"></li>
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown"><span class="caret"></span></a>
        <ul class="dropdown-menu">
          <li><%= link_to gravatar_for(current_user), user_path(current_user) %></li>
          <li><%= link_to current_user.name, user_path(current_user) %></li>
          <li><%= link_to "Settings", edit_user_path(current_user) %></li>
          <li class="divider"></li>
          <li><%= link_to "Log out", logout_path, method: :delete  %></li>    
        </ul>
      </li>
      <% else %>
        <li>
          <%= form_for :session, url: login_path, :html => {class: "navbar-form navbar-left", role: "form"}, remote: true  do |f| %>
              <%= f.text_field :email, placeholder: "Email", class: "form-control" %>
              <%= f.password_field :password, placeholder: "Passoword", class: "form-control"  %>

              <!-- <%= f.label :remember_me, class: "checkbox inline" do %>
                <%= f.check_box :remember_me %>
                <span>Remember me</span>
              <% end %> -->
            <%= f.submit "Login", class: "btn btn-primary" %>
          <% end %>
        </li>
      <% end %>
    </ul>
  </div>
</div>
</nav>
<br />
<br />
<br />