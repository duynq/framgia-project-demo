<% if entries.total_pages > 1 %>
	<%= form_for(user_path(user), method: :get, remote: true) do |f| %>
		<%= hidden_field_tag :page, entries.next_page %>
		<%= f.submit "Loading ...", class: "btn btn-info btn-block" %>
	<% end %>
<% end %>